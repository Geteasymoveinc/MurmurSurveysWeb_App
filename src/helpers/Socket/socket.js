/*import { io } from "socket.io-client";

const socket = io.connect("http://localhost:6446");

function SocketFunction() {
  console.log("socket");
  socket.on("connect", () => {
    console.log(socket.id, "Starting Socket Connection");
  });
  socket.io.on("error", (error) => {
    console.log(error);
  });
}

export { SocketFunction };*/
